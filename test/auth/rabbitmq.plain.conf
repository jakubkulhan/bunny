# RabbitMQ configuration for PLAIN authentication only
# This configuration disables AMQPLAIN and only allows PLAIN authentication

loopback_users = none
listeners.tcp.default = 5672

# Disable AMQPLAIN authentication mechanism, only allow PLAIN
auth_mechanisms.1 = PLAIN