# RabbitMQ configuration for AMQPLAIN authentication only
# This configuration disables PLAIN and only allows AMQPLAIN authentication

loopback_users = none
listeners.tcp.default = 5672

# Disable PLAIN authentication mechanism, only allow AMQPLAIN
auth_mechanisms.1 = AMQPLAIN